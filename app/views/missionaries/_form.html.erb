<%= simple_form_for(@missionary, html: { class: "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  <%= f.input :missbio, as: :text, input_html: { rows: "3" } %>  </div>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
