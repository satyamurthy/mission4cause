<div class="row">
    <div class="span6 offset3">
        <div class="well">
        	<%= image_tag @missionary.avatar %>
			<p>
			  <%= @missionary.missbio %>
			</p>
			<p>
			  <%= @missionary.user.name %>
			</p>
			<% if current_user == @missionary.user %>			
			<%= link_to 'Edit', edit_missionary_path(@missionary) %> |
			<% end %>
			<%= link_to 'Back', missionaries_path %>
		</div>
    </div>
</div>